//edit check to deterimine if date is within window
var DATE1;
var DATE2;
var timeDiff; //maximum length of window in ms

Math.abs(DATE1-DATE2) > timeDiff;


//if dates are separated by a certain time
var laterDate; //date should come after earlyDate
var earlyDate;
var diffBetweenDates; //time in ms that is between the two dates
var timeDiff; //maximum length of window in ms

Math.abs(earlyDate-(laterDate-diffBetweenDates)) > timeDiff